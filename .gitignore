.env
__pycache__/
*.py[cod]
.DS_Store
.cache/
.venv/

# Derived artifacts (embeddings, indexes) — regenerable offline.
artifacts/

# Evaluation run outputs. Keep the canonical artifact at submission/results.json.
runs/
